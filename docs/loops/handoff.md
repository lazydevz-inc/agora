# Alignment → Ralph Handoff Ceremony — Specification (Stage 2-C)

> **Status**: Stage 2-C in progress (opened 2026-05-03 after Stage 2-B close).
> Sections marked **[SPEC]** are formally accepted Stage 2-C outputs.
>
> Per ADR-0004, this document is not "Accepted" (full file) until Stage 2-C
> closes its gate. Stage 2-C close = full Stage 2 close.

---

## What the Handoff Is

When the Alignment Loop terminates (user assents in the Termination Gate),
the locked seed must transform into a state Ralph can iterate on:

```
Alignment Loop output:           Ralph Loop input:
  seed.acceptance_criteria       AC tree (.agora/ac_tree.json)
       (flat list)                 (decomposed, leaf-bearing, with provenance)
                ↓ HANDOFF CEREMONY ↓
                  Plato Dihairesis
                  + AC tree → Ralph state init
                  + handoff metadata write
```

The handoff is not a "translation step" — it is a structural transformation
that decomposes intent (acceptance criteria as written by the user) into the
operational unit Ralph iterates over (atomic AC nodes, each backed by a
generated test file per Stage 2-B.2).

This document specifies that ceremony.

---

## Phase Index

| Step | Status | Notes |
|------|--------|-------|
| **Plato Dihairesis decomposition** | **[SPEC]** | Accepted 2026-05-03 (Stage 2-C.1) |
| AC tree → Ralph state initialization | [OPEN] | Stage 2-C.2 next |
| Handoff metadata + audit | [OPEN] | Stage 2-C.3 |

---

## Plato Dihairesis Decomposition [SPEC] (Accepted 2026-05-03, Stage 2-C.1)

> **Goal**: Decompose `seed.acceptance_criteria` into the AC tree Ralph iterates
> on. Cuts at "natural joints" (per `docs/philosophy/04-plato-divided-line-and-dihairesis.md`)
> rather than convenient feature-list splits.

### Algorithm

```
plato_dihairesis_decompose(seed) -> ACTree:

  root = synthesize_root_from_telos(seed.telos, seed.acceptance_criteria)
    # Root is unified intent. Examples:
    #   "User can capture, retrieve, and connect reading notes"
    #   "Site renders all blog posts with consistent styling"
    # Generated by LLM with telos.statement + AC list as input.

  ac_tree = ACTree(root=root)
  recursive_decompose(root, depth=0)
  return ac_tree


recursive_decompose(node, depth):
  if is_atomic(node) OR depth >= MAX_DEPTH:
    mark_as_leaf(node)
    return

  # ── BINARY ARTICULATION ──
  proposed_binary = llm_propose_binary(node, context=seed)
    # Output: {
    #   binary_principle: "persistence vs association",
    #   child_a: AC("Notes persist with sufficient context"),
    #   child_b: AC("Notes are associated by user-meaningful links"),
    #   defense: "Persistence and association are independent operations
    #            (you can persist without linking, link without persisting).
    #            Cutting here separates concerns at their natural boundary."
    # }

  # ── BINARY DEFENSE ──
  alternatives = llm_generate_2_alternative_binaries(node, exclude=[proposed_binary])
  defense_score = llm_judge_defense(proposed_binary, alternatives)
    # 0.0 = alternatives equally natural / 1.0 = proposed clearly more natural

  if defense_score < DEFENSE_THRESHOLD:
    # Try N-ary fallback (3-way split, with defense)
    ternary = try_ternary_split(node, proposed_binary, alternatives)
    if ternary.defense_score >= DEFENSE_THRESHOLD:
      node.children = ternary.children  # 3-way exception per philosophy doc
      node.split_principle = ternary.principle
      node.split_defense = ternary.defense
      node.arity = 3
      for child in node.children:
        recursive_decompose(child, depth+1)
      return
    else:
      # Better an undivided AC than a badly divided one (philosophy doc principle)
      mark_as_leaf(node)
      return

  # ── ACCEPT BINARY → RECURSE ──
  node.children = [proposed_binary.child_a, proposed_binary.child_b]
  node.split_principle = proposed_binary.binary_principle
  node.split_defense = proposed_binary.defense
  node.arity = 2

  recursive_decompose(node.children[0], depth+1)
  recursive_decompose(node.children[1], depth+1)


MAX_DEPTH = 5            # R4-A — depth 5 force-leaf (Ouroboros heuristic carried)
DEFENSE_THRESHOLD = 0.6  # R3-A — 60% above alternatives = naturally articulable
```

### `is_atomic()` — composite criterion [R1-A]

```
is_atomic(ac) -> bool:
  # ALL THREE signals must pass
  llm_session_judgment   = llm_judge("Can a developer complete this in
                                      one focused coding session without
                                      context-switching?")
  estimated_file_touches = llm_estimate("How many distinct files would
                                         this likely touch?") <= 3
  conjunction_count      = count(ac.content matches "and|또는|그리고|+|,") <= 1

  return llm_session_judgment AND estimated_file_touches AND conjunction_count
```

The 3-AND criterion is intentionally conservative (favors smaller atomic
leaves rather than large ones). False-non-atomic (decomposing too deep) is
recoverable via user review (R2-A); false-atomic (leaving too coarse) often
requires re-running the whole Ralph session.

### Binary articulation prompt (LLM contract)

```
You are decomposing an acceptance criterion into its most fundamental
binary cut. Plato's principle: cut at natural joints, not convenient
feature lines.

Parent AC: {node.content}
Telos context: {seed.telos.statement}
Form context: {seed.form.essential_structure}

Identify ONE binary distinction that divides this parent AC into two
children. The distinction must be:
- Conceptually fundamental (not "feature 1 vs feature 2" but the underlying
  reason features 1 and 2 differ in kind)
- Cleanly separable (the two children should not significantly overlap)
- Defensible (you can articulate why this binary is more fundamental than
  alternatives)

Respond in JSON:
{
  "binary_principle": "<the binary in 'X vs Y' form>",
  "child_a": {
    "content": "<the AC representing X side>"
  },
  "child_b": {
    "content": "<the AC representing Y side>"
  },
  "defense": "<one paragraph: why this binary is fundamental,
              why the children don't overlap, why this is more
              natural than alternatives like Z or W>"
}
```

The `defense` field is then evaluated against alternative-binaries by a
separate LLM call (`llm_judge_defense`) producing the `defense_score`.

### Defense scoring [R3-A]

```
llm_judge_defense(proposed_binary, alternatives) -> float [0.0, 1.0]:
  prompt = """
  A decomposition algorithm proposed this binary cut for an AC:
    Proposed: {proposed_binary.binary_principle}
    Defense:  {proposed_binary.defense}

  Alternative binaries that could have been proposed:
    Alt 1: {alternatives[0].principle}
    Alt 2: {alternatives[1].principle}

  Judge: how clearly is the proposed binary MORE NATURAL than the alternatives?

  0.0 = alternatives are equally or more natural
  0.5 = proposed is slightly more natural, but not clearly so
  1.0 = proposed is clearly fundamental; alternatives are convenient cuts

  Respond with JSON: { "score": <float>, "reasoning": "<one sentence>" }
  """
  return llm.call(prompt).score
```

Threshold = 0.6 means "noticeably more natural than alternatives." Below
that, the algorithm tries ternary fallback or accepts leaf-as-is. Per
philosophy doc: *"Better an undivided AC than a badly divided one."*

### Max depth handling [R4-A]

```
MAX_DEPTH = 5

if depth >= MAX_DEPTH:
  # Force-leaf even if not atomic by other criteria
  mark_as_leaf(node)
  log_warning("Force-leaf at max depth: {node.id} — consider AC simplification")
```

Depth 5 is borrowed from Ouroboros's heuristic. Reasons:
- Tree depth >5 is hard for human review (R2-A user dialog becomes unwieldy)
- Each level introduces decomposition uncertainty; >5 levels = compounded uncertainty
- Stage 2-A.10 mini-alignment cascade traversal is bounded

If depth-5 force-leaves accumulate (>3 in one tree), `agora doctor` surfaces:
*"Tree has N force-leaves at max depth — your acceptance criteria may benefit
from simplification at the alignment level."*

### User review of generated tree [R2-A]

After `plato_dihairesis_decompose` completes and BEFORE Ralph begins, the user
sees the tree in an interactive review dialog:

```
─────────────────────────────────────────────────────────────────
  📊 AC Tree Generated (Plato Dihairesis)
─────────────────────────────────────────────────────────────────

  Root: "User can capture, retrieve, and connect reading notes"
    binary: persistence vs association  (defense: 0.78)
    │
    ├── child_a: "Notes persist with sufficient context"
    │     binary: internal context vs external context  (defense: 0.72)
    │     │
    │     ├── (leaf) "Capture command stores timestamp + book/url"
    │     └── (leaf) "Capture command stores user-typed reflection"
    │
    └── child_b: "Notes are associated by user-meaningful links"
          binary: explicit links vs inferred links  (defense: 0.81)
          │
          ├── (leaf) "User adds [[note-id]] syntax in capture body"
          └── (leaf) "Search results show co-occurring notes"

  Total: 4 leaf ACs (will become 4 .spec.ts test files per Stage 2-B.2)
  Max depth: 3 / 5
  Avg defense score: 0.77
  Force-leaves at max depth: 0

  ── Review the decomposition ──
    ◯  [Enter] Accept — proceed to test generation + Ralph
    ◯  [e] Edit specific node (opens interactive editor)
    ◯  [r] Re-decompose with explicit binary at node X (user-supplied principle)
    ◯  [b] Back to alignment (treat as termination dialog "Yes refine")

  > _
─────────────────────────────────────────────────────────────────
```

User actions:

- **[Enter] Accept**: tree is final. Test generation (Stage 2-B.2) starts immediately,
  then Ralph begins.
- **[e] Edit**: prompts for which node to edit. Opens inline editor with current
  node content + binary cut. User can rewrite. Children re-decomposed from new content.
- **[r] Re-decompose**: prompts for which node + the user's preferred binary
  principle. Algorithm uses user's principle (no defense check — user is the
  authority). Recurses with that as the seed.
- **[b] Back to alignment**: re-opens Termination Gate's "Yes refine" path.
  User can adjust the original ACs; new tree generated on re-handoff.

### Test file generation hooks here

Once the user accepts the tree (R2-A [Enter]):

```
on_tree_accepted(ac_tree):
  # 1. Persist tree
  write(.agora/ac_tree.json, ac_tree.serialize())

  # 2. Trigger Stage 2-B.2 initial test generation
  for leaf in ac_tree.leaves():
    generate_test_file(leaf)
    # Each leaf → .agora/tests/{leaf.id}.spec.ts
    # index.json updated with manifest

  # 3. State transition
  state.phase = "ready_for_ralph"

  # 4. Hand to Ralph entry
  print("✅ Tree locked. Tests generated.")
  print("Next: agora ralph to begin implementation.")
```

### Boundaries

- ❌ MAX_DEPTH > 5 (R4-C rejected): hard cap on review-ability and uncertainty compounding.
- ❌ MAX_DEPTH < 5 (would force premature leaf collapse on legitimately deep concepts).
- ❌ DEFENSE_THRESHOLD < 0.6 (R3-B rejected): would accept weakly-defended binaries
  (loses Plato's "natural joint" principle).
- ❌ DEFENSE_THRESHOLD > 0.6 (R3-C rejected): too strict; legitimate binaries
  fail score; tree stays shallow; large leaves pollute Ralph iterations.
- ❌ LLM-only atomicity judgment (R1-B rejected): single signal too brittle.
- ❌ Auto-progress without tree review (R2-B rejected): user must see + accept
  what Ralph will iterate over.
- ❌ Review only when tree is large (R2-C rejected): smaller trees are equally
  consequential per leaf; small trees just have larger per-leaf weight.
- ❌ N-ary > 3 (per philosophy doc): 4+ way splits indicate the proposed binary
  isn't really fundamental.
- ❌ Silent leaf mark (force-leaves logged + surfaced by agora doctor when accumulating).

### Output consumed by

- **Stage 2-B.2 Test generation**: receives `ac_tree.leaves()` for initial
  test file creation.
- **Ralph engine**: reads `.agora/ac_tree.json` for iteration ordering.
- **Stage 2-A.10 mini-alignment cascade**: SIBLING_REQUIREMENTS expansion
  follows the tree's parent-child relationships.
- **`agora status`**: renders tree progress (which leaves Ralph has greened).
- **`agora doctor`**: surfaces force-leaf accumulation, low average defense score.

### Failure modes specifically guarded

- **Convenient cuts disguised as natural** (philosophy F): defense scoring
  vs alternatives forces articulation; threshold prevents weak binaries.
- **Tree too deep for review**: MAX_DEPTH = 5 + force-leaf path.
- **Tree too shallow (lazy decomposition)**: 3-AND atomicity is conservative;
  is_atomic biased toward decomposition until composite signal aligns.
- **Silent acceptance of bad decomposition**: tree review (R2-A) is mandatory;
  no path skips it.
- **F-Aquinas-4 (silent overruling)**: every binary records `defense` text;
  `agora doctor` can surface trees where defense quality dropped.

---

## Open Questions for Stage 2-C

These resolve in Stage 2-C remaining sub-questions:

1. ~~**Plato Dihairesis decomposition algorithm**~~ ✅ Resolved 2026-05-03 (Stage 2-C.1). See above.

2. **AC tree → Ralph state initialization** (Stage 2-C.2) — open
   - How does `.agora/ac_tree.json` serialize?
   - In what order does Ralph iterate leaves? (depth-first? breadth-first?
     dependency-order from sibling requirements?)
   - When parallel (per ADR-0008), how do siblings divide the tree?
   - First-iteration starting leaf — leftmost? user-selectable?

3. **Handoff metadata + audit** (Stage 2-C.3) — open
   - What writes to `seed.metadata.handoff`?
   - Audit trail for force-leaves, low-defense binaries, user edits to tree
   - How does `agora resume` reconstruct in-progress handoff state?

---

*Stage 2-C close = full Stage 2 close.*
