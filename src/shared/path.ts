// SPEC: docs/architecture/module-graph.md (Stage 5-A.1)
//
// Skeleton placeholder. cwd resolution + .agora/ root finding land in
// the slice that needs them (Stage 6 — likely first config slice).

export {};
