// SPEC: docs/architecture/module-graph.md (Stage 5-A.1)
//
// Skeleton placeholder. Atomic file ops + JSON read/write helpers land in
// the slice that needs them (Stage 6 — likely first probe / config slice).

export {};
